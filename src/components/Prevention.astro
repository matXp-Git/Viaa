---
---

<section class="prevention">
  <div class="container">
    <p class="eyebrow">03</p>

    <h2 class="prevention__title">Préparer avant de faucher.</h2>

    <p class="prevention__lead">
      Viia Pick intervient en amont du fauchage pour retirer les déchets plastiques avant qu'ils ne
      soient fragmentés en microplastiques — une pollution ensuite impossible à extraire des eaux
      et des sols.
    </p>
  </div>
</section>

<style>
  .prevention {
    background: var(--color-accent);
    color: var(--color-black);
    padding-block: clamp(56px, 8vw, 88px);
  }

  .prevention .eyebrow {
    color: var(--color-black);
  }

  .prevention__title {
    margin-top: 24px;
    max-width: 780px;
    font-size: clamp(1.9rem, 3.4vw, 2.6rem);
    line-height: 1.2;
    font-weight: 400;
    text-transform: uppercase;
  }

  .prevention__lead {
    margin-top: 24px;
    max-width: 58ch;
    font-size: 1.05rem;
    line-height: 1.6;
  }
</style>
